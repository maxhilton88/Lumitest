/**
 * ModuleSelector.tsx — Shared card carousel for selecting subject modules.
 * Used by: Onboarding (ChildFlowPage) and Practice (PracticeScreen/RealmPracticePage).
 * Supports both sequential test mode and free-selection practice mode.
 *
 * Renamed from QuestSelector.tsx — "Quest" is now reserved for the future
 * Pokemon-style quest feature.
 */
export { ModuleSelector } from './QuestSelector';
